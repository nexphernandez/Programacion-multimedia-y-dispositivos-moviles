# tictactoe-back

Quick notes to install, run, and test the project.

Prerequisites
- Python 3.12
- uv

Run the app (development)
```bash
uv run python main.py
```

Run tests
```bash
uv run pytest
```